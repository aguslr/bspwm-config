#! /bin/sh

# Launch keybinding daemon
sxhkd &

# Launch systray
[ ! "`pgrep -u $USER stalonetray`" ] && stalonetray --config ~/.config/bspwm/stalonetrayrc &
# Hide systray after 5 seconds
( sleep 5s && xdotool search --class "stalonetray" windowunmap) &
# Enable hotcorner to toggle visibility of systray
xdotool behave_screen_edge --quiesce 600 --delay 0 bottom-right exec ~/.config/bspwm/scripts/toggle-systray &

# Start xautolock daemon
[ ! "`pgrep -u $USER xautolock`" ] && xautolock -locker "i3lock -de -I 5 -c 2d2d2d" -time 10 &

# Start Terminal daemon
[ ! "`pgrep -u $USER urxvtd`" ] && urxvtd --quiet --opendisplay &

# Start Udisks-glue for device automonting
[ ! "`pgrep -u $USER udisks-glue`" ] && udisks-glue &

# Launch panel
~/.config/bspwm/scripts/panel &

# Start Update-notifier
update-notifier &
# Start ownCloud client
owncloud &
# Start clipboard manager
parcellite &
# Start Empathy
empathy --start-hidden &
# Start lightsOn script
~/bin/lightsOn.sh 300 &
